---
import { getCollection } from "astro:content";
import ContentSection from "~/components/content-section.astro";
import ShowcaseCard from "~/components/showcase-card.astro";

const featuredSites = await getCollection(
  "showcase",
  ({ data }) => "featured" in data,
);
const orderedSites = featuredSites.sort(
  (a, b) => a.data.featured! - b.data.featured!,
);
---

<ContentSection title="Contacto" id="contact">
  <Fragment slot="lead">
    Ya sea que necesites  <span class="text-primary">asistencia</span> con tu compra, tengas consultas sobre nuestros productos o simplemente desees 
    <span class="text-primary">compartir tus comentarios</span>, 
    <span class="text-primary">estamos listos para ayudarte.</span>
    
  </Fragment>
  <div class="max-w-6xl space-y-2">
    <div class="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">
      <!-- {orderedSites.map((site) => <ShowcaseCard site={site} />)} -->
    </div>
    
    <form class="max-w-md mx-auto">
      <label for="name" class="block mb-2">Nombre:</label>
      <input type="text" id="name" name="name" required class="w-full px-4 py-2 mb-4 border border-gray-300 rounded-md">

      <label for="email" class="block mb-2">Email:</label>
      <input type="email" id="email" name="email" required class="w-full px-4 py-2 mb-4 border border-gray-300 rounded-md">

      <label for="message" class="block mb-2">Mensaje:</label>
      <textarea id="message" name="message" required class="w-full px-4 py-2 mb-4 border border-gray-300 rounded-md"></textarea>

      <button type="submit" class="px-4 py-2 bg-blue-500 text-white rounded-md">Enviar</button>
    </form>
  </div>
</ContentSection>
    
  </div>
</ContentSection>
